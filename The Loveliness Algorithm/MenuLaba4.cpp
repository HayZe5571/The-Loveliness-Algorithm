// Создать набор классов по теме «Плоские фигуры», одним из которых будет класс Circle (круг). Поле класса хранит радиус окружности. 
// Методы класса возвращают площадь, длину окружности, устанавливают радиус окружности и возвращение значения радиуса окружности, 
// выводят данные о полях класса на экран. Методы установки полей класса должны проверять корректность задаваемых параметров. В класс 
// добавить необходимый набор полей и методов (минимум два поля и два метода) на свое усмотрение. Создать метод для записи полученных 
// данных в файл. Создать методы доступа к полям класса.

#include "MyIncludes.h"
#include "HeaderLaba4.h"

extern vector<FlatFigure*> shapes;

extern void Laba4() {
    int choice;

    do {
        cout << "\n========== МЕНЮ ЛАБАРАТОРНОЙ №4 ==========" << endl;
        cout << "1. Добавить фигуры" << endl;
        cout << "2. Вывести все фигуры" << endl;
        cout << "3. Сохранить данные в файл" << endl;
        cout << "4. Загрузить данные из файла" << endl;
        cout << "5. Очистить все данные" << endl;
        cout << "6. Выход в главное меню" << endl;

        choice = chekInt("Выберите пункт", "Неверный выбор", [](int x) { return x >= 1 && x <= 6; });

        switch (choice) {
        case 1: addShape(); break;
        case 2: displayAllShapes(); break;
        case 3: saveToFileShapes("shapes_data.txt"); break;
        case 4: loadFromFileShapes("shapes_data.txt"); break;
        case 5: clearShapes(); break;
        case 6: cout << "Возврат в главное меню..." << endl; break;
        }
    } while (choice != 6);
}